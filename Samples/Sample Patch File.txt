# This is a sample patch file. None of the contents are actually valid.

@@file mcp/src/minecraft_server/net/minecraft/src/(file you edited).java

@@match
(Thing you want to find inside the file)
@@end

@@replace
(what to replace it with)
@@end
